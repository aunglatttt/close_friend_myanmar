@model IEnumerable<CloseFriendMyanamr.ViewModel.AOViewModel>

@{
    ViewData["Title"] = "Agent List";
}
<link rel="stylesheet" href="~/css/employeelist.css" asp-append-version="true" />
<div class="row">
    <div class="col-sm-12">
        <!-- Header Row: Title and Add Button -->
        <div class="page-header shadow">
            <h2>Agent Management</h2>
            <a class="btn btn-add" asp-controller="UserManagement" asp-action="AgentCreate">
                <i class="fas fa-user-plus"></i> Add New Agent
            </a>
        </div>

        <!-- Main Content Row -->
        <div class="row">
            <!-- Left Card: DataTable -->
            <div class="col-md-12 mb-4">
                <div class="card border-2 shadow">
                    <div class="card-header">
                        Agent List
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table id="employeeTable" class="table table-hover table-bordered" style="width:100%">
                                <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>Phone Number</th>
                                        <th>FB Page</th>
                                        <th>Remark</th>
                                        <th>Type</th>
                                        <th>No of Property</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.OwnerName</td>
                                            <td>@item.OwnerPhone</td>
                                            <td>@item.Address</td>
                                            <td>@item.Remark</td>
                                            <td>Agent</td>
                                            <td>@item.PropertyCount</td>
                                            <td>
                                                <i class="fa-solid fa-pen" onclick="window.location.href='@Url.Action("AgentCreate", "UserManagement", new { id = item.Id })'" style="cursor:pointer; color:#0279d4;" data-bs-toggle="tooltip" data-bs-placement="top" title="Edit"></i>
                                                <i class="fa-solid fa-trash-can" onclick="deleteIncomeTitle(@item.Id)" style="cursor:pointer; color:red; margin-left:10px;" data-bs-toggle="tooltip" data-bs-placement="top" title="Delete"></i>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
  
    </div>
</div>

<script>
    $(document).ready(function () {
        $('#employeeTable').DataTable({
            responsive: true, // Enable responsive feature
            paging: true,     // Enable pagination
            searching: true,  // Enable search
            ordering: true,   // Enable sorting
            info: true,       // Show table information
            autoWidth: false, // Disable automatic column width calculation
               lengthMenu: [      // Specify the length options
            [25, 50, 100, -1],  // -1 means 'All'
            [25, 50, 100, "All"] // Display labels for options
        ],
            columnDefs: [
                { responsivePriority: 1, targets: 0 }, // Make Employee Name column high priority
                { responsivePriority: 2, targets: -1 } // Make Action column high priority
            ]
        });
    });

     function deleteIncomeTitle(id) {
        if (confirm('Are you sure you want to delete this agent?')) {
            fetch(`/UserManagement/DeleteAgent/${id}`, { method: 'POST' })
                .then(() => window.location.reload());
        }
    }
</script>
